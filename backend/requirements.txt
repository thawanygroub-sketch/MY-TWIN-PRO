# ============================================================
# MyTwin Backend v18.1.0 – Production Requirements
# ============================================================
# متوافقة مع Expo SDK 52 + Python 3.11 + Railway
# ============================================================

# ── الأساسيات ─────────────────────────────────────────────
fastapi>=0.110.0,<0.116.0
uvicorn[standard]>=0.29.0,<0.31.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
pydantic>=2.0.0

# ── الذكاء الاصطناعي ─────────────────────────────────────
google-genai>=1.0.0
openai>=1.55.0
groq>=0.13.0
aiohttp>=3.9.0
httpx>=0.27.0

# ── قاعدة البيانات والتخزين ──────────────────────────────
supabase>=2.10.0
asyncpg>=0.29.0
redis>=5.0.0

# ── المراقبة والأداء ─────────────────────────────────────
sentry-sdk>=2.0.0
psutil>=5.9.6
tenacity>=8.2.0

# ── الصوت ─────────────────────────────────────────────────
edge-tts>=6.2.0

# ── معالجة الصور ─────────────────────────────────────────
Pillow>=10.0.0

# ── تحليل البيانات ───────────────────────────────────────
numpy>=1.24.0

# ── المصادقة الخارجية ────────────────────────────────────
google-auth>=2.0.0
google-api-python-client>=2.0.0

# ── معالجة الصوت (اختياري) ───────────────────────────────
vosk>=0.3.45

# ============================================================
# تثبيت: pip install -r requirements.txt
# ============================================================
slowapi>=0.1.9
